<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="NoActionBarBaseTheme" parent="Theme.AppCompat.Light.NoActionBar">

        <!-- customize the color palette -->
        <item name="colorPrimary">@color/blue_</item>
        <item name="colorPrimaryDark">@color/light_blue</item>
        <item name="colorAccent">@color/green</item>
        <item name="android:textColorPrimary">@color/gray1</item>
    </style>

</resources>
